import CreateTimetableForm from './create-timetable-form/create-timetable-form';

const CreateTimetable = () => (
  <div className="sign-up">
    <h1 className="logo">Beaurel</h1>

    <CreateTimetableForm />
  </div>
);

export default CreateTimetable;
